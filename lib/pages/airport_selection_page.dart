import 'package:flutter/material.dart';

class AirportSelectionPage extends StatelessWidget {
  const AirportSelectionPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
